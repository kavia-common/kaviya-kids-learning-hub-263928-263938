# Kaviya Frontend

This app uses Create React App and React Router.

Environment setup:
- Copy .env.example to .env and set REACT_APP_BACKEND_URL to your backend (e.g., http://localhost:3001).
- If not set, API client falls back to relative URLs and the UI will use local mock data where applicable.

Scripts:
- npm start
- npm run build
- npm test
